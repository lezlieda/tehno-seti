spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tehno_seti_db
    username: tehno_user
    password: Hfleuf7&
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

app:
  file-storage: C:\Users\user\Documents\tehnoseti\uploads